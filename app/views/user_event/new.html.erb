<div>
  <h1 class="font-bold text-4xl">UserEvent#new</h1>
  <p>Sign in for event <%= params[:event_id]%></p>
  <p>Current user id: <%= User.find_by(email: current_admin.email).id %></p>

  <%# <%form_with url: user_event_path, local: true, method: post do%>
  <%# <%end%>

</div>
